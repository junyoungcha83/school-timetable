name = "school-timetable-api"
main = "src/index.js"
compatibility_date = "2025-05-01"

# KV namespace — 다음 명령으로 생성한 뒤 id 를 채우세요:
#   npx wrangler kv:namespace create TIMETABLE
[[kv_namespaces]]
binding = "TIMETABLE"
id = "25816f25ef1f476d8ce184e1b9a0570e"

# 편집 비밀번호는 secret 으로:
#   npx wrangler secret put EDIT_TOKEN
